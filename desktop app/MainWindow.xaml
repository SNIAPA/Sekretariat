<Window x:Class="desktop_app.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:desktop_app"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="MainWindow"
        Height="485" Width="800" 
        
        >
    <Grid Margin="0,0,0,0"
        >

        <Menu  VerticalAlignment="Top" Background="LightGray" BorderBrush="Gray" BorderThickness="1" >
            <MenuItem Header="Dane"  BorderBrush="Gray" BorderThickness="1">
                <MenuItem x:Name="ImportButton" Header="_Import"/>
                <MenuItem x:Name="ExportButton" Header="_Export"/>
            </MenuItem>
            <MenuItem Header="Help"  BorderBrush="Gray" BorderThickness="1">
                <MenuItem x:Name="FilterHelpButton" Header="_Filter"/>
            </MenuItem>
        </Menu>
        <TabControl Height="390" VerticalAlignment="Top" Margin="0,23">
            <!--                                STUDENTS                                   -->
            <TabItem Header="uczniowie">

                <DataGrid AutoGenerateColumns="False" 
                          Height="360"
                          HorizontalAlignment="Left" 
                          Name="student_list_grid" 
                          VerticalAlignment="Top" 
                          CanUserSortColumns="True"
                          CanUserDeleteRows="True"
                          RowHeight="50" 
                          Width="537">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Imie" Binding="{Binding Path=first_name}" MinWidth="50" />
                        <DataGridTextColumn Header="Nazwisko" Binding="{Binding Path=last_name}" MinWidth="50" />
                        <DataGridTextColumn Header="Imie ojca" Binding="{Binding Path=fathers_name}" MinWidth="50" />
                        <DataGridTextColumn Header="Imie matki" Binding="{Binding Path=mothers_name}" MinWidth="50" />
                        <DataGridTextColumn Header="plec" Binding="{Binding Path=gender}" MinWidth="50" />
                        <DataGridTextColumn Header="pesel" Binding="{Binding Path=pesel}" MinWidth="50" />
                        <DataGridTemplateColumn Header="Data ur">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <DatePicker SelectedDate="{Binding Path=birth_date}"></DatePicker>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Zdjecie" MaxWidth="50">
                            <DataGridTemplateColumn.CellTemplate >
                                <DataTemplate>
                                    <Image Width="50" Source="{Binding Path=photo}"></Image>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Path=photo}"></TextBox>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Command="Delete" Content="delete"></Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                    
                </DataGrid>
            </TabItem>
            <!--                                TEACHERS                                     -->
            <TabItem Header="nauczyciele">

                <DataGrid AutoGenerateColumns="False" 
                          Height="360"
                          HorizontalAlignment="Center" 
                          Name="teacher_list_grid" 
                          VerticalAlignment="Top" 
                          CanUserSortColumns="True"
                          CanUserDeleteRows="True"  
                          Width="537"
                          RowHeight="50">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Imie" Binding="{Binding Path=first_name}" MinWidth="50" />
                        <DataGridTextColumn Header="Nazwisko" Binding="{Binding Path=last_name}" MinWidth="50" />
                        <DataGridTextColumn Header="Imie ojca" Binding="{Binding Path=fathers_name}" MinWidth="50" />
                        <DataGridTextColumn Header="Imie matki" Binding="{Binding Path=mothers_name}" MinWidth="50" />
                        <DataGridTextColumn Header="plec" Binding="{Binding Path=gender}" MinWidth="50" />
                        <DataGridTextColumn Header="pesel" Binding="{Binding Path=pesel}" MinWidth="50" />
                        <DataGridTemplateColumn Header="Data ur">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <DatePicker SelectedDate="{Binding Path=birth_date}"></DatePicker>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Zdjecie" MaxWidth="50">
                            <DataGridTemplateColumn.CellTemplate >
                                <DataTemplate>
                                    <Image Width="50" Source="{Binding Path=photo}"></Image>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Path=photo}"></TextBox>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Command="Delete" Content="delete"></Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <!--                                    GROUPS                                -->
            <TabItem Header="groups">
                <DataGrid AutoGenerateColumns="False" 
                          Height="360"
                          HorizontalAlignment="Center" 
                          Name="group_list_grid" 
                          VerticalAlignment="Top" 
                          CanUserSortColumns="True"
                          CanUserDeleteRows="True"  
                          Width="537"
                          RowHeight="50">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Imie" Binding="{Binding Path=first_name}" MinWidth="50" />
                        <DataGridTextColumn Header="Nazwisko" Binding="{Binding Path=last_name}" MinWidth="50" />
                        <DataGridTextColumn Header="Imie ojca" Binding="{Binding Path=fathers_name}" MinWidth="50" />
                        <DataGridTextColumn Header="Imie matki" Binding="{Binding Path=mothers_name}" MinWidth="50" />
                        <DataGridTextColumn Header="plec" Binding="{Binding Path=gender}" MinWidth="50" />
                        <DataGridTextColumn Header="pesel" Binding="{Binding Path=pesel}" MinWidth="50" />
                        <DataGridTemplateColumn Header="Data ur">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <DatePicker SelectedDate="{Binding Path=data_ur}"></DatePicker>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="Zdjecie" MaxWidth="50">
                            <DataGridTemplateColumn.CellTemplate >
                                <DataTemplate>
                                    <Image Width="50" Source="{Binding Path=photo}"></Image>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <TextBox Text="{Binding Path=photo}"></TextBox>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Command="Delete" Content="delete"></Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
        <Button  Content ="Filter" Margin="24,0,0,5" x:Name="testButton" VerticalAlignment="Bottom" HorizontalAlignment="Left" Height="22" RenderTransformOrigin="0.5,0.5" Width="58"></Button>
        <Button  Content ="Reset" Margin="100,0,0,5" x:Name="ResetButton" VerticalAlignment="Bottom" HorizontalAlignment="Left" Height="22" RenderTransformOrigin="0.5,0.5" Width="58"></Button>
        <TextBox  Margin="200,0,0,5" x:Name="filterBox" VerticalAlignment="Bottom" HorizontalAlignment="Left" Height="22" Width="300"></TextBox>
        
    </Grid>
</Window>
